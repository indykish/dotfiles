{
  "name": "orly-dev",
  "description": "Development manifest for the orly sources. The published package is the repository root; this file carries only the toolchain the tests and typecheck need.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test src",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
