''.join(random.choice(chars) for _ in range(size))
import random
import string
''.join(random.choice(chars) for _ in range(size))
chars=string.ascii_uppercase + string.digits
''.join(random.choice(chars) for _ in range(size)) + "@rioos.sh"
''.join(random.choice(chars) for _ in range(7)) + "@rioos.sh"
''.join(random.choice(chars) for _ in range(7)).lower() + "@rioos.sh"
"mok_'+'.join(random.choice(chars) for _ in range(7)).lower() + "@rioos.sh"
'mok_'+'.join(random.choice(chars) for _ in range(7)).lower() + "@rioos.sh"
'mok_'+''.join(random.choice(chars) for _ in range(7)).lower() + "@rioos.sh"
'space_'+''.join(random.choice(chars) for _ in range(7)).lower() + "@rioos.sh"
'sachin_'+''.join(random.choice(chars) for _ in range(7)).lower() + "@rioos.sh"
exit
import numpy as np
;
arr = [[1.,0.,0.],[0.,1.,2.]]
print arr
print(arr)
b = np.nparray(shape(arr))
b = np.ndarray(shape(arr))
b = np.ndarray.shape(arr)
b = arr.shape
c = np.array(arr)
c.shape
exit
a = sum([x*x for x in range(10)])
;
print a
print(a)
sum(x*x for x in range(10))
print(a.class)
print(a.clasz)
import jwt
encoded_jwt = jwt.encode({'some': 'payload'}, 'secret', algorithm='HS256')
encoded_jwt
help
help()
imports
modules
exit
import jwt
exit
ls
import jwt
import pyjwt.jwt
import PyJWT.jwt
import jwt
