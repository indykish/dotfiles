---
description: Package the current work state for the next agent or future session
argument-hint: [FOCUS="<optional scope or recipient>"]
---

Use the `handoff` skill now.

Treat `$ARGUMENTS` as optional focus. Include every section required by the
skill even when focus is supplied.
