---
description: Rehydrate repository context and continue with the next safe actions
argument-hint: [FOCUS="<optional workstream or handoff>"]
---

Use the `pickup` skill now.

Treat `$ARGUMENTS` as optional focus. Rehydrate the complete repository state
before continuing with safe, in-scope actions.
